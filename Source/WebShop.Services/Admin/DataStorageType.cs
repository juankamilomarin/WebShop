namespace WebShop.Services.Admin
{
    /// <summary>
    /// Enum to describe the types of data storage
    /// </summary>
    public enum DataStorageType
    {
        Persistent = 0,
        Memory = 1
    }
}
